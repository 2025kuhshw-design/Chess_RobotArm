# Simulation-only extras (PyBullet URDF viewer / sim training).
# Not needed for real robot operation. Install requirements.txt first.
#
# NOTE: On Windows pybullet has no prebuilt wheel for some Python versions and
# falls back to source build, which needs Microsoft C++ Build Tools. If you do
# not have the build tools, skip this file and run the real robot only.
pybullet
