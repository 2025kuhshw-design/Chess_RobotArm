# 시뮬레이션(PyBullet URDF 뷰어 / sim 학습)에만 필요한 추가 패키지.
# 실제 로봇 작동에는 불필요하다. 먼저 requirements.txt 를 설치한 뒤 필요할 때만.
#
# ⚠️ Windows에서 pybullet은 사전빌드 wheel이 없으면 소스 컴파일을 시도하며
#    Microsoft C++ Build Tools가 필요하다(설치 안 되어 있으면 실패).
#    빌드 도구가 없으면 이 파일은 건너뛰고 실제 로봇 작동만 진행하면 된다.
pybullet
